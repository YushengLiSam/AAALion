# Copy to `.env` in server/ before running. NEVER commit a real .env.

# Doubao (provided by competition; shared in team channel)
DOUBAO_BASE_URL=https://ark.cn-beijing.volces.com/api/v3/
DOUBAO_MODEL_ID=ep-20260514111645-lmgt2
DOUBAO_API_KEY=replace-me

# Vector DB
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION_TEXT=products_text
QDRANT_COLLECTION_IMAGE=products_image

# Server
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
LOG_LEVEL=INFO

# iOS client points at this base URL (also baked into client/.../Config.swift)
PUBLIC_BACKEND_URL=http://localhost:8000
